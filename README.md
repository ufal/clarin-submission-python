## clarin-submission-python
DSpace python library to allow ingestion of metadata with files to create new submission.

### Environment variables used by library
Example:
```
AUTHORIZATION_TOKEN = ey....0uYw
DSPACE_API_ENDPOINT = 'https://lindat.mff.cuni.cz/repository/server/api'
DSPACE_COLLECTION_ID = 34352d2c-3296-4448-aeb5-d18f2e179126
SUBMISSION_DEFINITION_NAME = traditional
```
Where:

**AUTHORIZATION_TOKEN** is the persistence token used for authentication. The token can be created by admin or by submitter  

**DSPACE_API_ENDPOINT** is the base URL for DSpace Server API Endpoints

**DSPACE_COLLECTION_ID** is the collection UUID string, specifying the (parent) collection where the submission data will be stored  

**SUBMISSION_DEFINITION_NAME** is the submission definition name used by the collection. The submission definition specifies the submission metadata form used in submission requests

All of these environment variables will be given to user by admin.  
Note that every script allows to override these environment variables by providing the corresponding command-line arguments:
- **-t, --token** to override AUTHORIZATION_TOKEN
- **-e, --dspace-api-endpoint** to override DSPACE_API_ENDPOINT
- **-c, --collection-id** to override DSPACE_COLLECTION_ID
- **-d, --submission-definition-name** to override SUBMISSION_DEFINITION_NAME

### Create Submission Metadata Template file (CSV format)

```
python generate_submission_metadata_template.py [-h] [-m SUBMISSION_METADATA]
                                                [-t TOKEN]
                                                [-e DSPACE_API_ENDPOINT]
                                                [-s SUBMISSION_DEFINITION_NAME]
                                                [-r RESOURCE_TYPE]

Command-line arguments

options:
  -h, --help            show this help message and exit
  -m, --submission-metadata SUBMISSION_METADATA
                        Template name for submission metadata, in CSV format
                        (optional). Default: submission.csv
  -t, --token TOKEN     Authorization token, or use the AUTHORIZATION_TOKEN
                        env variable
  -e, --dspace-api-endpoint DSPACE_API_ENDPOINT
                        DSpace API Endpoint, or use the DSPACE_API_ENDPOINT
                        env variable
  -d, --submission-definition-name SUBMISSION_DEFINITION_NAME
                        Submission Definition Name, or use the
                        SUBMISSION_DEFINITION_NAME env variable
  -r, --resource-type RESOURCE_TYPE
                        Resource Type (optional), sample values: corpus
                        (default), lexicalConceptualResource,
                        languageDescription, toolService
```

Example:
```
python generate_submission_metadata_template.py -m /User/John/Submissions/submission-data.csv -r toolService
```
This command creates a **submission-data.csv** file, in **/Users/John/Submissions** directory, for submissions with **toolService** resource type.

### Submission Metadata Template file (CSV format)

File generated by previous command, can be completed by adding the metadata values for individual keys.

Example:
<pre>
__section__,traditionalpageone
dc.type[required=true type=enum(corpus|lexicalConceptualResource|languageDescription|toolService)],<b>toolService</b>
dc.title[required=true],<b>DEMO Submission</b>
dc.source.uri[type=URL],
local.demo.uri[type=URL],
dc.relation.isreferencedby[repeatable=true type=URL],
dc.date.issued[required=true type=date format=&lt;dddd-mm-dd&gt;],<b>2025-12-20</b>
dc.publisher[required=true repeatable=true],
dc.contributor.author[required=true repeatable=true],<b>John Lizard,Mark Hagues,"Emily, Smith Lion"</b>
local.contact.person[required=true repeatable=true type=complex format=&lt;givenname:text;surname:text;email:text;affiliation:text&gt;],<b>John;Lizard;john.lizard@email.com;Institute of Foreign Languages</b>
local.sponsor[repeatable=true type=complex format=&lt;type:enum(N/A|euFunds|ownFunds|nationalFunds|Other);code:text;orgname:text;projname:text&gt;],<b>nationalFunds;1122;UFAL;Project X</b>

__section__,traditionalpagetwo
dc.description[required=true],
dc.language.iso[repeatable=true],
dc.subject[required=true repeatable=true],
metashare.ResourceInfo#ContentInfo.detailedType[required=true type=enum(tool|service|platform|suiteOfTools|infrastructure|architecture|nlpDevelopmentEnvironment|other)],<b>service</b>
metashare.ResourceInfo#ResourceComponentType#ToolServiceInfo.languageDependent[required=true type=enum(true|false)],<b>false</b>

__section__,specialFields
local.submission.note[],
dc.relation.replaces[repeatable=true],

__section__,license
granted[required=true type=enum(true|false)],<b>true</b>

__section__,clarin-license
__enum_value__,"GNU General Public Licence, version 3"
__enum_value__,"GNU General Public License, version 2"
__enum_value__,The MIT License (MIT)
__enum_value__,Artistic License 2.0
__enum_value__,Artistic License (Perl) 1.0
__enum_value__,Attribution-NonCommercial-NoDerivs 3.0 Unported (CC BY-NC-ND 3.0)
__enum_value__,"BSD 2-Clause ""Simplified"" or ""FreeBSD"" license"
__enum_value__,"BSD 3-Clause ""New"" or ""Revised"" license"
__enum_value__,Attribution-NonCommercial 3.0 Unported (CC BY-NC 3.0)
__enum_value__,Attribution-NonCommercial-ShareAlike 3.0 Unported (CC BY-NC-SA 3.0)
__enum_value__,Attribution-NoDerivs 3.0 Unported (CC BY-ND 3.0)
__enum_value__,Attribution-ShareAlike 3.0 Unported (CC BY-SA 3.0)
__enum_value__,Creative Commons - Attribution 3.0 Unported (CC BY 3.0)
__enum_value__,Public Domain Dedication (CC Zero)
__enum_value__,Apache License 2.0
__enum_value__,Affero General Public License 1 (AGPL-1.0)
__enum_value__,Affero General Public License 3 (AGPL-3.0)
__enum_value__,Common Development and Distribution License (CDDL-1.0)
__enum_value__,Eclipse Public License 1.0 (EPL-1.0)
__enum_value__,"GNU Library or ""Lesser"" General Public License 2.1 (LGPL-2.1)"
__enum_value__,"GNU Library or ""Lesser"" General Public License 2.1 or later (LGPL-2.1)"
__enum_value__,"GNU Library or ""Lesser"" General Public License 3.0 (LGPL-3.0)"
__enum_value__,Mozilla Public License 2.0
__enum_value__,Open Data Commons Attribution License (ODC-By)
__enum_value__,Open Data Commons Open Database License (ODbL)
__enum_value__,Open Data Commons Public Domain Dedication and License (PDDL)
__enum_value__,Public Domain Mark (PD)
__enum_value__,Creative Commons - Attribution-NoDerivatives 4.0 International (CC BY-ND 4.0)
__enum_value__,Creative Commons - Attribution-NonCommercial 4.0 International (CC BY-NC 4.0)
__enum_value__,Creative Commons - Attribution-NonCommercial-NoDerivatives 4.0 International (CC BY-NC-ND 4.0)
__enum_value__,GNU General Public License 2 or later (GPL-2.0)
__enum_value__,Creative Commons - Attribution 4.0 International (CC BY 4.0)
__enum_value__,Creative Commons - Attribution-NonCommercial-ShareAlike 4.0 International (CC BY-NC-SA 4.0)
__enum_value__,Creative Commons - Attribution-ShareAlike 4.0 International (CC BY-SA 4.0)
__enum_value__,Oracle Specific License
__enum_value__,Milan's License
name[type=enum],<b>"GNU General Public Licence, version 3"</b>
</pre>

Note, here, only the following metadata values are defined: **dc.type, dc.title, dc.date.issued, dc.contributor.author, local.contact.person, local.sponsor, metashare.ResourceInfo#ContentInfo.detailedType** or **license.granted** and **clarin-license.name** where
- **dc.type** (single) value is automatically pre-defined during metadata template generation
- **dc.title** (single) value is defined by user
- **dc.contributor.author** (list) values are defined by user
- **local.contact.person** and **local.sponsor** are complex field values defined by user (see that complex field values are separated by semicollon)
- **metashare.ResourceInfo#ContentInfo.detailedType** and **metashare.ResourceInfo#ResourceComponentType#ToolServiceInfo.languageDependent** are values defined by user (see that these values are of enum type)
- **license.granted** and **clarin-license.name** are pre-defined values during metadata template generation, but user can change those values (by selecting value from enum list)

So, this way user may define any number of metadata values, where each metadata value can be set as a single value or a list of values.  
The **\_\_section\_\_** lines define the individual submission sections, where the metadata belong to

### Submission Metadata + Files Upload (metadata in CSV format)

```
python upload_submission.py [-h] [-m SUBMISSION_METADATA] [-s SUBMISSION_ID]
                            [-f FILES [FILES ...]] [-t TOKEN]
                            [-e DSPACE_API_ENDPOINT] [-c COLLECTION_ID]

Command-line arguments

options:
  -h, --help            show this help message and exit
  -m, --submission-metadata SUBMISSION_METADATA
                        Submission metadata file name, in CSV format
                        (optional). Default: submission.csv
  -s, --submission-id SUBMISSION_ID
                        submission ID (optional), if provided, metadata +
                        files will be uploaded to this submission
  -f, --files FILES [FILES ...]
                        Files to upload (optional)
  -t, --token TOKEN     Authorization token, or use the AUTHORIZATION_TOKEN
                        env variable
  -e, --dspace-api-endpoint DSPACE_API_ENDPOINT
                        DSpace API, or use the DSPACE_API_ENDPOINT env
                        variable
  -c, --collection-id COLLECTION_ID
                        DSpace Collection ID, or use the DSPACE_COLLECTION_ID
                        env variable
```

Example 1:
```
python upload_submission.py -m /User/John/Submissions/submission-data.csv -f articles.zip sample-video.mp4 logo.png
```

In this case new submission will be created with metadata defined in **submission-data.csv** file and
3 files (bitstreams): **articles.zip, sample-video.mp4, logo.png** will be uploaded to created submission.

Example 2:
```
python upload_submission.py -s 7401 -m /User/John/Submissions/new-submission-data.csv -f new-video.mp4
```

In this case existing submission with ID 7401 will be updated with metadata defined in **new-submission-data.csv**
file and one video file **new-video.mp4** will be uploaded to this submission.

### Submission File(s) upload

```
python upload_submission_files.py [-h] -s SUBMISSION_ID -f FILES [FILES ...]
                                  [-t TOKEN] [-e DSPACE_API_ENDPOINT]

Command-line arguments

options:
  -h, --help            show this help message and exit
  -s, --submission-id SUBMISSION_ID
                        submission ID (required)
  -f, --files FILES [FILES ...]
                        Files to upload (required)
  -t, --token TOKEN     Authorization token (optional), r use the
                        AUTHORIZATION_TOKEN env variable
  -e, --dspace-api-endpoint DSPACE_API_ENDPOINT
                        DSpace API Endpoint (optional), or use the
                        DSPACE_API_ENDPOINT env variable
```


Example:
```
python upload_submission_files.py -s 7401 -f new-logo.png examples.zip
```

In this case two files (bitstreams): **new-logo.png** and **examples.zip** will be uploaded to existing submission with ID 7401.
